# RCOM Lab 2 - Computer Networks

> to run the program you gotta run on terminal the following codes :
```
$ make clean && make
```
>
> after that you may want to use `$ make help` to see all the commands.
> 
> To run program you can check the make help options for the preset tests or run `$ ./download <url>` with the specified URL you want to run


